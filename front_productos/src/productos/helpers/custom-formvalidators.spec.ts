import { CustomFormvalidators } from './custom-formvalidators';

describe('CustomFormvalidators', () => {
  it('should create an instance', () => {
    expect(new CustomFormvalidators()).toBeTruthy();
  });
});
